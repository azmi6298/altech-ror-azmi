<div class="w-full flex flex-col gap-8">
  <div class="flex flex-col text-2xl text-center">
    <span class="text-4xl font-semibold mb-8"><%= @country.name %> <%= @country.emoji %></span>
    <span>Code: <%= @country.code %></span>
    <span>Native: <%= @country.native %></span>
    <span>Capital: <%= @country.capital %></span>
    <span>Currency: <%= @country.currency %></span>

    <span class="mt-24"><%= link_to "Back to home", root_path %></span>
  </div>
</div>